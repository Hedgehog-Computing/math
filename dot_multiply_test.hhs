/**
 * @author Jason Reynolds
 * @param
 * @returns
 * 
 * Function for testing dot_divide.hhs
 */


function dot_multiply_test() {

    *import math: dot_multiply

    let a = new Mat([[3, 4], [5, 6]]);
    let b = [[2, 2], [1, 3]];
    if (!(dot_multiply(a, b) === [[6, 8], [5, 18]])) {
        throw new Error('Unit test failed for dot_divide');
    }
}